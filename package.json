{
  "name": "gulp-test",
  "version": "1.0.0",
  "description": "gulp test",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "hwang",
  "license": "ISC",
  "devDependencies": {
    "gulp": "^4.0.2"
  }
}
